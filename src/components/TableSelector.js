// Placeholder per futura UI di selezione/configurazione tabelle personalizzate
export function renderTableSelector(tabelle, onChange) {
    const container = document.createElement('div');
    container.className = 'table-selector';
    return container;
}